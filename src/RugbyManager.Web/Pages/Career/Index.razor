@page "/career"
@inject GameService Game
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo(Game.Career is null ? "career/new" : "career/dashboard", replace: true);
    }
}
