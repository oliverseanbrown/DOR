namespace RugbyManager.Web.Services;

/// <summary>How much playback lingers on dramatic moments (line breaks, tries, cards, etc.).</summary>
public enum CommentaryPace
{
    Fast,
    Normal,
    Cinematic,
}
